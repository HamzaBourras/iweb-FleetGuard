# ===============================================================================
# Fichier     : requirements.txt
# Rôle        : Déclaration des bibliothèques et dépendances Python
# Description :
#    Liste exhaustive des paquets externes nécessaires au fonctionnement du backend. 
#    Inclut le framework web (FastAPI, Uvicorn), l'ORM et son pilote BDD (SQLAlchemy, 
#    psycopg2-binary), la pile cryptographique (cryptography, passlib, python-jose, pyotp), 
#    les tâches d'arrière-plan (apscheduler) et le client HTTP asynchrone (httpx).
# ===============================================================================

fastapi
uvicorn
sqlalchemy
psycopg2-binary
pydantic
cryptography>=42.0.0
passlib[bcrypt]
python-jose[cryptography]
httpx>=0.27.0
apscheduler
pyotp
python-dotenv